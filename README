docker部署
	1、上传安装包到服务器：
		1)、使用ssh 访问153.35.99.131 服务器 port 2048 ,登陆 docker 服务器
        用户名:docker_guest_1 密码: guest
		2)、二进制模式传送
			a)discovery-eureka-0.0.1-SNAPSHOT.jar,Dockerfile
			  到/home/docker_guest_1/discovery；
			b)provider-user-0.0.1-SNAPSHOT.jar,Dockerfile
			  到/home/docker_guest_1/user
			注：两个Dockerfile内容不同
	2、部署
		1、cd /home/docker_guest_1/discovery
		2、docker build -t cyw/discovery . （出现Successfully built a7cc6f4de088时成功）
		3、cd /home/docker_guest_1/user
		4、docker build -t cyw/user .      （出现Successfully built a7cc6f4de088时成功）
		注：Successfully built后的字母数字实际值会不同
		
启动服务
	1、153.35.99.131 服务器启动
		1、使用ssh 访问153.35.99.131 服务器 port 2048 ,登陆 docker 服务器
		   用户名:docker_guest_1 密码: guest
	       执行命令：docker run -p 8761:8761 discovery-eureka
		2、再开一个ssh客户端，使用相同的用户登陆
	       执行命令：docker run -p 8000:8000 provider-user
	2、eclipse工程启动
		1、运行 /discovery-eureka/src/main/java/com/bjzkar/cloud/EurekaApplication.java
    2、运行 /provider-user/src/main/java/com/bjzkar/application/UserApplication.java
		注：第一条成功启动后再启动第二条

主页说明
  1、登陆页面：在153.35.99.131服务器上 访问 http://localhost:8000/login.html 访问登陆页面
		1)登陆：输入用户名、密码。点击"登陆"按钮，登陆。（页面：校验用户名和密码不能为空；后台：校验用户名密码是否正确）
		2)注册：点击"注册"按钮，进入注册页面。输入用户名、密码、密码确认，点击"注册"按钮。（页面：校验所有内容不能为空，后台：用户能是否可用）
	2、用户信息修改页面：
		修改：
			1)登陆成功后会自动跳转到修改页面，后台会校验是否登陆，如果没有登陆会自动跳转到登陆页面。
			2)校验已登陆后修改页面会反显：用户名、电话、邮箱。
			3)可以在登陆页面修改电话、邮箱、密码。原密码为必输项。如果输入新密码则会校验新密码是否和原密码相同。
			4)点"提交"按钮，提交修改。后台会校验是否登陆、用户名与密码是否正确，如果均校验通过则保存修改。
			5)成功后修改页面会刷新显示修改后的信息。
		删除:
			1)删除用户时，原密码是必输项，点击"删除用户"按钮。后台会校验是否登陆、用户名与密码是否正确，如果均校验通过则保存删除用户信息。
			2)成功删除后会跳转到登陆页面。
      
数据源说明
	1)数据库：h2
	2)测试数据 cloud_user
  drop table cloud_user if exists;
	create table cloud_user (
		user_id bigint generated by default as identity, --用户Id
		user_name varchar(100),                          --用户名
		user_password varchar(100),                      --登陆密码
		user_telephone varchar(50),                      --电话
		user_email varchar(100),                         --邮箱
		primary key (user_id)                            --用户Id为主键
	);
	
	insert into cloud_user (user_id, user_name, user_password, user_telephone, user_email)
	values (1, 'Tom', '123456', '18612345678', 'Tom@126.com');     

	insert into cloud_user (user_id, user_name, user_password, user_telephone, user_email)
	values (2, 'Aaron', '123456', '18612345678', 'Aaron@126.com');

	insert into cloud_user (user_id, user_name, user_password, user_telephone, user_email)
	values (3, 'Basil', '123456', '18612345678', 'Basil@126.com');

	insert into cloud_user (user_id, user_name, user_password, user_telephone, user_email)
	values (4, 'Colbert', '123456', '18612345678', 'Colbert@126.com');

对于主要的java文件，需要指出并注释
	1)发现服务启动 /discovery-eureka/src/main/java/com/bjzkar/cloud/EurekaApplication.java
	2)用户服务启动 /provider-user/src/main/java/com/bjzkar/application/UserApplication.java
	3)用户服务Controller
	  /provider-user/src/main/java/com/bjzkar/application/controller/user/UserController.java
	  /provider-user/src/main/java/com/bjzkar/application/controller/user/logined/LoginedUserController.java
	4)用户服务Service
	  /provider-user/src/main/java/com/bjzkar/application/service/UserService.java
	  /provider-user/src/main/java/com/bjzkar/application/service/UserServiceImpl.java
